# A dictionary to store config parameters and their possible values
config_updates = {
    "robot.policy": ['orca', 'social_force', 'srnn', 'selfAttn_merge_srnn'],  # Example: change robot.policy
    "env.test_size": [2]
}